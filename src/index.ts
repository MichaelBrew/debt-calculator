import { calculateInterestChargedForMonth } from "./util/interest";
import { calculatePayoffTimeline } from "./util/debt";

export default {
  calculateInterestChargedForMonth,
  calculatePayoffTimeline,
}
